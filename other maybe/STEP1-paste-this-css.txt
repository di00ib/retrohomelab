============================================================
STEP 1 OF 2 — PASTE THIS CSS INTO THE HEART JOURNAL APP
============================================================

WHERE TO PASTE:
  Open heartjournal/index.html on GitHub.
  Find the line near the very bottom of the <style> block that says:
    @media(max-width:600px){...}
  Paste the CSS block BELOW that @media line, just before the closing </style> tag.

============================================================
PASTE THE FOLLOWING CSS:
============================================================

/* ===== FEEDBACK WIDGET ===== */
.fb-btn{
  position:fixed;bottom:24px;right:24px;
  background:linear-gradient(135deg,var(--rose),var(--rose2));
  color:#fff;border:none;border-radius:50px;
  padding:12px 20px;font-size:.85rem;font-weight:700;
  cursor:pointer;display:flex;align-items:center;gap:7px;
  box-shadow:0 4px 20px rgba(181,41,78,.45);
  font-family:var(--fbody);
  transition:all .2s ease;z-index:500;
  letter-spacing:.2px;
}
.fb-btn:hover{transform:translateY(-2px);box-shadow:0 6px 28px rgba(181,41,78,.6);}
.fb-btn svg{width:16px;height:16px;flex-shrink:0;}
.fb-overlay{
  position:fixed;inset:0;background:rgba(0,0,0,.65);
  z-index:2000;display:flex;align-items:center;justify-content:center;
  padding:16px;backdrop-filter:blur(6px);
  opacity:0;pointer-events:none;transition:opacity .22s ease;
}
.fb-overlay.open{opacity:1;pointer-events:all;}
.fb-modal{
  background:var(--surface);border:1px solid var(--border);border-radius:20px;
  padding:28px 26px;width:100%;max-width:480px;
  box-shadow:0 16px 60px rgba(0,0,0,.5);
  transform:translateY(16px);transition:transform .25s ease;
  max-height:90vh;overflow-y:auto;
}
.fb-overlay.open .fb-modal{transform:translateY(0);}
.fb-modal-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;}
.fb-modal-title{font-family:var(--fhead);font-size:1.2rem;font-weight:700;color:var(--text);display:flex;align-items:center;gap:8px;}
.fb-close{background:none;border:none;color:var(--text3);font-size:1.4rem;cursor:pointer;line-height:1;padding:2px 6px;border-radius:6px;transition:color .15s;}
.fb-close:hover{color:var(--text);}
.fb-subtitle{font-size:.83rem;color:var(--text2);margin-bottom:20px;line-height:1.5;}
.fb-field{display:flex;flex-direction:column;gap:5px;margin-bottom:14px;}
.fb-label{font-size:.72rem;font-weight:700;text-transform:uppercase;letter-spacing:.6px;color:var(--text3);}
.fb-opt{font-weight:400;color:var(--text3);text-transform:none;letter-spacing:0;opacity:.7;}
.fb-input,.fb-select,.fb-textarea{
  width:100%;padding:10px 13px;border:1.5px solid var(--border);
  border-radius:10px;background:var(--warm);color:var(--text);
  font-family:var(--fbody);font-size:.92rem;
  transition:border-color .18s,box-shadow .18s;-webkit-appearance:none;
}
.fb-input:focus,.fb-select:focus,.fb-textarea:focus{
  outline:none;border-color:var(--rose);
  box-shadow:0 0 0 3px var(--rose-pale);
}
.fb-textarea{resize:vertical;min-height:90px;}
.fb-row{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
.fb-submit{
  width:100%;padding:12px;border:none;border-radius:10px;
  background:linear-gradient(135deg,var(--rose),var(--rose2));
  color:#fff;font-size:.95rem;font-weight:700;cursor:pointer;
  font-family:var(--fbody);
  transition:all .2s;margin-top:4px;
  display:flex;align-items:center;justify-content:center;gap:8px;
}
.fb-submit:hover:not(:disabled){transform:translateY(-1px);box-shadow:0 4px 16px rgba(181,41,78,.4);}
.fb-submit:disabled{opacity:.6;cursor:not-allowed;transform:none;}
.fb-success{text-align:center;padding:24px 10px;display:none;}
.fb-success-icon{font-size:3rem;margin-bottom:12px;}
.fb-success-title{font-family:var(--fhead);font-size:1.2rem;font-weight:700;color:var(--text);margin-bottom:8px;}
.fb-success-msg{font-size:.88rem;color:var(--text2);line-height:1.6;}
.fb-error{
  background:rgba(181,41,78,.1);border:1px solid rgba(181,41,78,.25);
  border-radius:8px;padding:10px 13px;font-size:.83rem;color:var(--stage2);
  margin-top:10px;display:none;line-height:1.5;
}
@media(max-width:480px){
  .fb-btn{bottom:100px;right:16px;padding:11px 16px;font-size:.8rem;}
  .fb-row{grid-template-columns:1fr;}
}

============================================================
END OF CSS — now go to STEP2-paste-this-html.txt
============================================================
